//Model object contains all dataclasses, attributes, dataclass methods//Programatic dataclass//model.NewClass = new DataClass('NewClass','public');//model.NewClass.ID = new Attribute('storage','string');//This js file is included in Model.jsmodel.Travelers.methods.calcBalances = function() {	ds.Travelers.forEach(function(doUpdate) {		var lineTotal = 0;		doUpdate.paymentsCollection.orderBy("trxDate asc");		doUpdate.paymentsCollection.forEach(function(doCalc) {//			if(doCalc.chgPmt = null) {//				doCalc.chgPmt = false;//			};			lineTotal = lineTotal + doCalc.trxLocalAmt;			doCalc.balOwing = lineTotal;		});	});};model.Travelers.methods.calcBalances.scope='public';//forEach{name:varName},function(e) {//	e.userData.name = varName;//});//varName+='test';